#include<stdio.h>
int sum_digits_recur(int n)
{
    if(n==0)
    return;
    int d = n%10;
    return d + sum_digits_recur(n/10);
}
void main()
{
    int n;
    printf("enter n\n:");
    scanf("%d",&n);
    printf("%d\n",sum_digits(n));
}